<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movies</title>
  </head>
  <body>
    <h1>Movies</h1>
    <dl>
        <% @movie_list.each do |movie| %>
          <dt><a href="/movies/<%= movie["id"] %>"><%= movie["title"] %></a> </dt>
            <dd>Year Released: <%=movie["year"]%></dd>
            <dd>Rating: <%=movie["rating"]%></dd>
            <dd>Studio: <%=movie["name"]%></dd>
            <dd>Genre: <%=movie["genre"]%> </dd>
        <% end %>
    </dl>
  </body>
</html>
